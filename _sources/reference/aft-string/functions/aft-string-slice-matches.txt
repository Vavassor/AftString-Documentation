aft_string_slice_matches
========================

.. c:function:: bool aft_string_slice_matches(AftStringSlice a, \
        AftStringSlice b)

    Determine if two slices' contents are exactly the same.

    :param a: the first slice
    :param b: the second slice
    :return: true if the slices match
