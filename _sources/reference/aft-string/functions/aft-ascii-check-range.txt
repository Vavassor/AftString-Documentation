aft_ascii_check_range
=====================

.. c:function:: bool aft_ascii_check_range(const AftString* string, \
        const AftStringRange* range)

    Determine if a range within a string is :term:`ASCII` encoded.

    :param string: the string
    :param range: the range
    :return: true if the range is ASCII-only

